// Importing the 'constants' and 'schemas' modules from their respective files
const constants = require('./constants');
const schemas = require('./schemas');

// Exporting an object containing the 'constants' and 'schemas' modules
module.exports = {
  constants,
  schemas
};
